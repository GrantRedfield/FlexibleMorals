{
  "name": "flexiblemorals-unified",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm run -ws install",
    "dev:all": "npm-run-all -p dev:backend dev:frontend",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend start",
    "build:all": "npm-run-all -s build:backend build:frontend",
    "build:backend": "npm --workspace backend run build || echo \"(skip if JS only)\"",
    "build:frontend": "npm --workspace frontend run build",
    "lint:all": "npm-run-all -s lint:*",
    "lint:backend": "npm --workspace backend run lint || echo \"no backend lint script\"",
    "lint:frontend": "npm --workspace frontend run lint || echo \"no frontend lint script\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
