# ===========================================
# FlexibleMorals Frontend Environment Variables
# ===========================================
# Vite requires env vars to start with VITE_ to be exposed to the client

# Backend API URL
# Leave BLANK for local development — the Vite proxy handles forwarding to
# localhost:3001, which also lets phones on the same WiFi access the site.
# Only set this for production builds (e.g. https://api.flexiblemorals.org).
# VITE_API_URL=
VITE_API_URL=

# Giphy API Key (free at https://developers.giphy.com/dashboard/)
# Required for GIF search in chat
VITE_GIPHY_API_KEY=
